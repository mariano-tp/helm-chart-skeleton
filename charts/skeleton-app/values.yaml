# Overrides de nombre (opcionales)
nameOverride: ""
fullnameOverride: ""

replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

serviceAccount:
  create: true
  name: ""        # si lo dejás vacío usa el fullname
  automount: true # default sensato; lo valida el schema

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}
